# Hostinger production environment variables template for crossword.network
# Set these in Hostinger hPanel → Node.js App → Environment Variables.
# Do NOT commit real secrets.

NODE_ENV=production
NEXT_PUBLIC_APP_URL=https://crossword.network
NEXT_TELEMETRY_DISABLED=1

# Optional (if server RAM is tight). Tune to your Hostinger plan.
# NODE_OPTIONS=--max-old-space-size=512

# NextAuth
NEXTAUTH_URL=https://crossword.network
NEXTAUTH_SECRET=REPLACE_WITH_STRONG_RANDOM_SECRET

# Database
DATABASE_URL=mysql://USER:PASSWORD@HOST:PORT/DATABASE

# Email (Resend)
RESEND_API_KEY=REPLACE_WITH_RESEND_API_KEY
EMAIL_FROM=Crossword Network <noreply@crossword.network>

# OAuth (Google)
GOOGLE_CLIENT_ID=REPLACE_WITH_GOOGLE_CLIENT_ID
GOOGLE_CLIENT_SECRET=REPLACE_WITH_GOOGLE_CLIENT_SECRET

# Hostinger runtime bind
BIND_HOST=127.0.0.1
